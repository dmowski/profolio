name: welcome-page-Test

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  run-linters:
    name: welcome-page-test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - working-directory: ./main-services/welcome-page
      - run: npm ci
      - run: npm lint      
  
  test:
    name: welcome-page-lint
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - working-directory: ./main-services/welcome-page
      - run: npm ci
      - run: npm run test